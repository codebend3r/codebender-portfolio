node_modules
dist
coverage
package-lock.json

# Emitted by the composite tsconfig.node.json project during `ts:check`
*.tsbuildinfo

# Gale's incremental lint cache (only written with `gale --cache`)
.gale_cache

# Local Netlify folder
.netlify

# Generated by netlify CLI's Deno runtime during `netlify dev`
deno.lock

# Brainstorming mockups (visual companion)
.superpowers

.env
public/cv/

# Local knowledge-graph output (graphify skill)
graphify-out/
